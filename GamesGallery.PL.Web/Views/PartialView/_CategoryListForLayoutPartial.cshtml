@model List<string>

@if (Model != null && Model.Count() > 0)
{
    foreach (string category in Model)
    {
        @Html.ActionLink(category, "Category", "Games", new { category = category }, new { @class = "dropdown-item" })
    }
}
